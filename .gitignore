__pycache__/
*.pyc
.venv/
venv/
.env

# Ignore the data dir (live DB, cached bars, trained model) but ship the two
# portable, version-controlled artifacts: the news cache + the training set.
data/*
!data/news_seed.csv
!data/training.csv

# The live database and any model binary are per-machine / regenerable.
*.sqlite
*.db
